# orbit-message-bus

NATS JetStream wrapper — subject naming, envelope enforcement, retry (max 3), DLQ. Status: `beta`. MIT — `github.com/manovaspace/orbit-message-bus`.

## Commands

```bash
go test ./...
```

Local NATS: `nats://localhost:10422` (ADR-006).
